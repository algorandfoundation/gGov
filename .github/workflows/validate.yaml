name: Pull Request validation

on: [pull_request]


jobs:
  contracts-ci:
    name: Run contracts validation
    uses: ./.github/workflows/contracts-ci.yaml

  frontend-ci:
    name: Run frontend validation
    uses: ./.github/workflows/frontend-ci.yaml

